[package]
name = "agent-identity"
version = "0.1.0"
description = "On-chain agent identity storage and evolution for Solana"
edition = "2021"

[lib]
crate-type = ["cdylib", "lib"]
name = "agent_identity"

[features]
default = []
cpi = ["no-entrypoint"]
no-entrypoint = []
no-idl = []
no-log-ix-name = []
idl-build = ["anchor-lang/idl-build"]

[dependencies]
anchor-lang = { version = "0.31.1" }
